# FilmYears UI设计规范文档

> 产品：FilmYears｜iOS /iPadOS，SwiftUI
> 风格定位：**复古胶片极简主义 Minimal Film‑Vintage**
> 核心气质：安静、怀旧、克制，低刺激；拒绝花哨装饰；胶片颗粒作为纹理而非强视觉；主叙事载体是用户照片记忆。
> 模式：支持 **浅色 / 深色模式**；深色优先（主推）；适配动态类型 Dynamic‑Type、StandBy、锁屏小组件。
> 
> 

## 1\. 设计原则

1. **记忆优先**：UI 不能抢夺照片注意力，控件低存在感。

2. **胶片隐喻克制**：齿孔、颗粒是细节纹理，不要大面积做旧、划痕、复古滤镜。

3. **无焦虑叙事**：绝不出现倒计时、剩余时间相关图形文案；只展示已经发生的日期。

4. **一致性**：App 内、小组件复用同一套组件、颜色、胶片 Shader。

5. **无障碍优先**：颜色不作为唯一信息区分；支持动态字体。

## 2\. 色彩系统

### 主调色板（Dark Mode 优先）

|角色|色值‑深色|色值‑浅色|用途|
|---|---|---|---|
|背景 Background|`#121214`|`#F7F6F3`|页面底层背景|
|卡片 / 底片底色 Film Base|`#1E1E22`|`#EBE9E4`|胶片帧、胶卷列表项底色|
|主文本 Primary Text|`#F5F5F7`|`#1C1C1E`|大标题、正文|
|次要文本 Secondary Text|`#98989D`|`#636366`|辅助说明、计数、时间、占位文字|
|强调色 Accent‑WarmOrange|`#E29568`|`#D17848`|高亮、Focus 激活齿孔、按钮强调、年轮亮部；低饱和暖橙，不刺眼|
|分割线 Divider|`#2C2C30`|`#D3D2CD`|列表分割|
|水印灰色 Watermark|`#444448`|`#B8B7B2`|免费版导出水印文字颜色|

> 规则：
> 
> 1. 强调色只用做点缀，**不要大面积铺满**；
> 
> 2. 胶片齿孔高亮仅在 Focus 授权且活跃时启用，默认使用次要文本色；
> 
> 3. 年轮可视化亮度变化，基于 Accent‑WarmOrange 做透明度渐变，不新增额外色相。
> 
> 

## 3\. 字体系统 — SF 系列（iOS 系统字体，不引入第三方字体）

全部使用 Apple 原生 San Francisco，适配 Dynamic‑Type。

|层级|字体|字重|使用场景|
|---|---|---|---|
|大标题 LargeTitle|SF Pro|Semibold|页面导航标题「FilmYears」|
|标题 Title1|SF Pro|Bold|胶卷年份、弹窗日期标题|
|标题 Title2|SF Pro|Semibold|模块小标题、小组件主文字|
|正文 Body|SF Pro|Regular|备注、普通说明文字|
|辅助文本 Caption|SF Pro|Regular|帧计数、小组件次要文案、图例|

约束：

1. 备注最大显示一行，超出截断；**MVP 不做多行大段文本**。

2. 小组件文字尽量简短，避免拥挤溢出。

## 4\. 胶片组件规范（全局核心组件，App\+Widget 共用）

### 4\.1 FilmFrame 胶片帧组件（网格里的每一天）

> 尺寸：网格使用自适应 LazyVGrid，iPhone 竖屏 4 列；iPad 6‑8 列。
> 
> 

- **外框**：圆角 `8pt`；四周胶片齿孔装饰（左右两侧小半圆齿孔图形，非边框线条）。

- **底片底色**：Film Base 色；叠加全局胶片颗粒 shader（低强度噪点，透明度 8%‑12%，不要很重颗粒）。

- **空白帧状态**：底片底色，无图片；齿孔为次要文本色；无额外图标。

- **已填充帧状态**：

    - 图片 content‑mode `.fill` 裁剪铺满底片区域；

    - 图片上层叠加**低强度胶片颗粒 shader**；

    - 底部 1 行截断备注，Caption 字号，半透明黑色遮罩仅在底部；

- **Focus 活跃状态**：左右齿孔颜色切换为 Accent‑WarmOrange；仅齿孔变色，图片本身不变色。

- **ScreenTime 分数映射**：只调整**胶片颗粒强度**（0‑20% 区间），不改变色相。

> ❗禁止：给照片自动套复古滤镜，照片保留原始观感，颗粒仅作为叠加纹理。
> 
> 

### 4\.2 FilmRollListItem 胶卷列表项（首页）

- 高度：`88pt`；圆角 `12pt`

- 布局：左侧迷你胶片缩略图形；右侧：年份 Title2 \+ 次要文本 “已填充 XX 帧”

- 底色：Film Base；轻微颗粒纹理

- 点击反馈：SwiftUI 列表自带按压变暗效果

## 5\. 页面布局规范

### 通用边距（iOS 标准）

- 页面左右安全边距：`16pt`

- 组件间距：

    - 网格帧之间间距：`10pt`

    - 列表行间距：`12pt`

    - 弹窗内部元素垂直间距：`16pt`

### 5\.1 胶卷首页 FilmRollList

1. 导航栏：标准 NavigationBar；标题「FilmYears」右上角齿轮设置图标。

2. List 分组，每一行是 FilmRollListItem。

3. 无空状态插画；无未来年份条目。

### 5\.2 胶片卷轴 FilmReelView

1. 顶部导航栏：返回按钮｜年份标题｜切换年轮视图按钮（图标：圆环图标）

2. LazyVGrid 网格展示 FilmFrame 组件，垂直滚动。

3. 无页眉页脚装饰，减少干扰。

### 5\.3 FrameEditSheet 底部编辑弹窗 sheet

- 系统 bottom sheet；圆角系统默认。

- 从上到下：日期标题 → PhotosPicker 照片选择区 → 照片预览 → 单行备注输入框（max 80 字符）→ 删除照片按钮 → 底部【取消 / 保存】。

- 输入框单行；不支持多行大文本。

### 5\.4 RingView 年轮可视化视图

> 使用 Canvas 绘制，**禁止大量 View 堆叠圆环**。
> 
> 

1. 导航栏：返回 \+「多年总览」开关 Toggle。

2. 画布占页面大部分空间，上下留边距。

3. 同心圆年轮：

    - 外圈 = 更近的年份；

    - 角度分段对应一年 365 天；

    - 有记录的片段：Accent‑WarmOrange，透明度随状态变化；空白片段：低透明度底片灰色；

4. 底部极简图例文字（Caption），不做复杂图标；

5. 交互：点击圆环片段，定位跳转对应胶片帧。

### 5\.5 SettingsView 设置页

- SwiftUI 分组 List `Form`；标准 iOS 设置样式。
分组：

1. iCloud 同步

2. 系统权限：ScreenTime / Focus 模式跳转

3. 外观：浅色 / 深色自动切换

4. 购买：买断解锁、恢复购买

5. 其他：隐私政策链接、导出入口

## 6\. Widget 小组件 UI 规范（三套小组件）

> 全部小组件复用：胶片齿孔组件 \+ 同一个胶片颗粒 shader；支持 systemSmall /systemMedium；StandBy 使用 medium 布局。
> 
> 

### 6\.1 Today‑Frame 今日帧（主推 medium）

**空白状态**

- 胶片齿孔边框，底片底色 \+ 颗粒；居中文字：`Today’s Frame · Add a photo`；次要文本颜色。

**已填充状态**

- 胶片框内部展示照片，底部半透明遮罩；一行文字：日期｜简短备注（截断）。

- widgetURL 统一：`widget://open‑today‑frame`

> 小组件限制：**小组件内部禁止任何写操作；全部修改跳回主 App 完成**。
> 
> 

### 6\.2 Random‑Frame 随机帧 medium

- 胶片组件展示随机一张已填充照片；底部小字：对应日期。点击跳转对应帧。

### 6\.3 Year‑Ring 年轮缩略 small

- 小型简化年轮圆环；无文字，纯图形概览。

> 锁屏小组件：极简模式，只展示状态提示文字，**不渲染大图**。
> 
> 

## 7\. 空状态 \& 限制提示 UI

1. 胶卷无填充帧：卷轴网格只显示大量空白胶片帧，**不展示大插画**；次要文本底部小字提示：「添加照片与备注记录你的日子」。

2. 免费版达到 60 帧上限：尝试新增照片时弹窗提示：“已到达免费版 60 帧上限，一次性买断解锁无限帧”，提供跳转购买按钮。

3. ScreenTime/Focus 权限未授权：UI 不报错，胶片不渲染额外纹理，不弹强制提醒，设置页入口可主动开启。

## 8\. 图标规范

- 使用 SF Symbols 系统图标，不做自定义手绘图标。

- 使用图标：gear（设置）、arrow\.backward（返回）、circle（年轮切换）、photo（相册选择）。

- 图标颜色：次要文本色；按钮点击时切换为强调色。

## 9\. 导出图片 UI 规范

1. 导出胶片帧图片：带胶片齿孔 \+ 颗粒效果。

2. 免费版：右下角浅灰色水印文字 `FilmYears`；买断版移除水印。

3. 年轮导出图片保持 App 内配色。

## 10\. 适配规则

1. **动态类型 Dynamic‑Type**：文本全部支持放大；网格在文字过大时自动降低列数，保证不挤压。

2. iPad：边距放大；胶片网格列数增加。

3. 深色模式是主推，浅色模式完整适配全部组件。

4. StandBy：小组件适配黑色底色，降低颗粒强度，保证可见性。

## 11\. 禁止清单（UI 红线）

1. ❌ 不要做划痕、撕边、旧纸张、浓烈复古滤镜；颗粒只是微弱叠加纹理。

2. ❌ 禁止出现任何生命倒计时、剩余周数、死亡相关图形与文案。

3. ❌ 不要把 Accent 暖橙大面积铺满背景，仅做点缀高亮。

4. ❌ 胶片帧内照片不要强制加滤镜，保留原图观感。

5. ❌ 不要预渲染未来年份 / 未来日期胶片帧。

---

如果你需要，我可以输出：

1. 一份可复制到 Figma 的样式变量表；

2. 补充一份交 UI 设计师的交付说明。

> （注：部分内容可能由 AI 生成）
